plugins {
	id 'java'
	id 'org.springframework.boot' version '3.3.0'
	id 'io.spring.dependency-management' version '1.1.5'
}

version = '0.0.1-SNAPSHOT'

java {
	sourceCompatibility = JavaVersion.VERSION_17
	targetCompatibility = JavaVersion.VERSION_17
}

repositories {
	mavenCentral()
}

//ext {
//	set('springCloudVersion', "2023.0.2")
//}

dependencies {
	implementation 'org.springframework.cloud:spring-cloud-config-server'
	implementation 'org.springframework.boot:spring-boot-starter-actuator'
	testImplementation 'org.springframework.boot:spring-boot-starter-test'
	testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

dependencyManagement {
	imports {
		mavenBom "org.springframework.cloud:spring-cloud-dependencies:${versionSpringCloud}"
	}
}

jar {
	enabled = false
}

bootJar {
	manifest {
		attributes 'Start-Class': 'ua.igorg.userfusioncloudconfigserver.UserFusionCloudConfigServerApplication'
	}
	enabled = true
	archiveBaseName = "${project.name}"
	duplicatesStrategy = 'exclude'
}

tasks.named('test') {
	useJUnitPlatform()
}

tasks.register("prepareKotlinBuildScriptModel") {}

//tasks.withType(Checkstyle).configureEach {
//	configDirectory.set(srcDirs)
//}